# BrandClave Aggregator Dependencies

# Core
pydantic>=2.0
pydantic-settings>=2.0
alembic>=1.13
psycopg[binary]>=3.1  # Typed settings validated at boot (config/settings.py)
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0
chromadb>=0.4.0

# Scraping
httpx>=0.25.0
beautifulsoup4>=4.12.0
feedparser>=6.0.0
lxml>=4.9.0

# NLP
mistralai>=1.0.0,<2.0  # 2.x moved the client out of the top-level package
langdetect>=1.0.9
textblob>=0.17.0
sentence-transformers>=2.2.0  # Local fallback for embeddings
hdbscan>=0.8.33  # Clustering for trend detection

# API
fastapi>=0.109.0
uvicorn>=0.27.0

# Auth
pyjwt>=2.8

# Image generation (brand concept renders)
openai>=1.50

# Utilities
pyyaml>=6.0

# Phase 5: Caching & Scheduling
redis>=4.0.0
APScheduler>=3.10.0

# Active Inference (JAX-based v1.x line)
# Was pinned to the git tag v1.0.0_alpha, which upstream deleted once 1.0.0 shipped.
# The 1.x line is the same JAX API the POMDP code targets: infer_policies(qs),
# sample_action(..., rng_key=...). Now installed from PyPI, no git clone needed.
inferactively-pymdp>=1.0.0,<2.0
jax>=0.4.0
jaxlib>=0.4.0
